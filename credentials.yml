# Battery Smart Voicebot - Channel Credentials
# https://rasa.com/docs/rasa-pro/connectors/messaging-and-voice-channels/

# REST Channel - Primary interface for voice orchestrator
rest:
  # No credentials needed for REST channel

# WebSocket Channel - For real-time voice streaming
socketio:
  user_message_evt: user_uttered
  bot_message_evt: bot_uttered
  session_persistence: true

# Rasa Enterprise Interface
rasa:
  url: "http://34.218.254.232:5002/api"

# Voice Channel Configuration
# Note: Voice is handled through Twilio Media Streams + Deepgram + Rasa
# The voice orchestrator connects via REST channel

# Twilio Integration (configured via environment variables)
# - TWILIO_ACCOUNT_SID
# - TWILIO_AUTH_TOKEN
# - TWILIO_PHONE_NUMBER

# Deepgram Integration (configured via environment variables)
# - DEEPGRAM_API_KEY
